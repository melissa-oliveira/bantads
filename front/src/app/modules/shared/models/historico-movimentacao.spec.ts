import { HistoricoMovimentacao } from './historico-movimentacao';

describe('HistoricoMovimentacao', () => {
  it('should create an instance', () => {
    expect(new HistoricoMovimentacao()).toBeTruthy();
  });
});
