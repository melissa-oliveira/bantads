import { IsNumericoDirective } from './is-numerico.directive';

describe('IsNumericoDirective', () => {
  it('should create an instance', () => {
    const directive = new IsNumericoDirective();
    expect(directive).toBeTruthy();
  });
});
