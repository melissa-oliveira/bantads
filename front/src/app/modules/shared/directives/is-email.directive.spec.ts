import { IsEmailDirective } from './is-email.directive';

describe('IsEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEmailDirective();
    expect(directive).toBeTruthy();
  });
});
