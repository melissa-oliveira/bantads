import { IsCpfDirective } from './is-cpf.directive';

describe('IsCpfDirective', () => {
  it('should create an instance', () => {
    const directive = new IsCpfDirective();
    expect(directive).toBeTruthy();
  });
});
