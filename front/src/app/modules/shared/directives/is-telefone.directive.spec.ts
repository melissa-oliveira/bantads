import { IsTelefoneDirective } from './is-telefone.directive';

describe('IsTelefoneDirective', () => {
  it('should create an instance', () => {
    const directive = new IsTelefoneDirective();
    expect(directive).toBeTruthy();
  });
});
