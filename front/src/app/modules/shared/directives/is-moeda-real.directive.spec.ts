import { IsMoedaRealDirective } from './is-moeda-real.directive';

describe('IsMoedaRealDirective', () => {
  it('should create an instance', () => {
    const directive = new IsMoedaRealDirective();
    expect(directive).toBeTruthy();
  });
});
