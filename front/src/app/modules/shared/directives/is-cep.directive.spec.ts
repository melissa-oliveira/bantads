import { IsCepDirective } from './is-cep.directive';

describe('IsCepDirective', () => {
  it('should create an instance', () => {
    const directive = new IsCepDirective();
    expect(directive).toBeTruthy();
  });
});
