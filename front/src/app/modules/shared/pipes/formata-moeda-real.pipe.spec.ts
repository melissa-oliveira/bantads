import { FormataMoedaRealPipe } from './formata-moeda-real.pipe';

describe('FormataMoedaRealPipe', () => {
  it('create an instance', () => {
    const pipe = new FormataMoedaRealPipe();
    expect(pipe).toBeTruthy();
  });
});
